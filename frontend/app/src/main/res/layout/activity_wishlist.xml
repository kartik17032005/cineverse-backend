<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="#121212"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <!-- Header Title -->

    <!-- Empty State Message, initially hidden -->

    <TextView
        android:id="@+id/wishlistHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:fontFamily="@font/poppins_semibold"
        android:paddingBottom="12dp"
        android:text="💖 Your Wishlist"
        android:textColor="#00FFF7"
        android:textSize="26sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/wishlistEmptyMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your wishlist is empty. Start adding your favorite movies!"
        android:textColor="#888C9B"
        android:textSize="16sp"
        android:textAlignment="center"
        android:visibility="gone"
        android:paddingVertical="30dp"
        android:fontFamily="@font/poppins_regular" />

    <!-- RecyclerView to show wishlisted movies -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/wishlistRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="16dp"
        android:overScrollMode="never" />

</LinearLayout>
