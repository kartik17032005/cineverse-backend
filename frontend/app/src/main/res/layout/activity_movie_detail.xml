<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A1028"
    android:fillViewport="true"
    android:fitsSystemWindows="true"
    android:paddingTop="36dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Poster Section with Back & Play buttons -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="460dp"
            android:clipChildren="false">

            <!-- Poster Image (lowest in Z) -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/detailPoster"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerCrop"
                android:contentDescription="Movie Poster"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:shapeAppearanceOverlay="@style/RoundedPosterBottom32dp"
                app:strokeColor="#3B74FD50"
                app:strokeWidth="2dp"
                android:elevation="2dp" />

            <!-- Gradient Overlay (drawn above the poster, below buttons) -->
            <View
                android:id="@+id/gradientOverlay"
                android:layout_width="0dp"
                android:layout_height="160dp"
                android:background="@drawable/fade_gradient"
                app:layout_constraintBottom_toBottomOf="@id/detailPoster"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Back Button (drawn last so always visible) -->
            <ImageButton
                android:id="@+id/backButton"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:contentDescription="Back"
                android:src="@drawable/ic_back"
                android:background="@drawable/glass_circle_bg"
                android:elevation="12dp"
                android:layout_marginTop="16dp"
                android:layout_marginStart="16dp"
                app:tint="#00FFF7"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- Play Button (centered at bottom, last so always on top) -->
            <ImageButton
                android:id="@+id/btnWatchTrailer"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="22dp"
                android:background="@drawable/glow_cyan_circle"
                android:contentDescription="Play Trailer"
                android:elevation="12dp"
                android:padding="6dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_play_button"
                app:layout_constraintBottom_toBottomOf="@id/detailPoster"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/detailTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="36dp"
            android:layout_marginBottom="10dp"
            android:text="War of the Worlds"
            android:textColor="#FFFFFF"
            android:textSize="28sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.04"
            android:shadowColor="#33000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="6" />

        <!-- Rating, Genre, Year Section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="18dp"
            android:layout_marginEnd="18dp"
            android:layout_marginBottom="24dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/glass_bg"
            android:padding="16dp"
            android:elevation="4dp">

            <TextView
                android:id="@+id/detailRating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⭐ 8.8"
                android:textColor="#FFD700"
                android:textSize="16sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium" />

            <View
                android:layout_width="18dp"
                android:layout_height="1dp" />

            <TextView
                android:id="@+id/detailGenre"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Action • Sci-Fi"
                android:textColor="#56C8E9"
                android:textSize="15sp"
                android:fontFamily="sans-serif-light"
                android:layout_marginEnd="14dp"
                android:layout_marginStart="0dp" />

            <View
                android:layout_width="14dp"
                android:layout_height="1dp" />

            <TextView
                android:id="@+id/detailYear"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2025"
                android:textColor="#B0B0B0"
                android:textSize="15sp"
                android:fontFamily="sans-serif-light" />
        </LinearLayout>

        <!-- Description -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="18dp"
            android:layout_marginEnd="18dp"
            android:layout_marginBottom="38dp"
            android:backgroundTint="#33FFFFFF"
            app:cardCornerRadius="18dp"
            app:cardElevation="3dp">

            <TextView
                android:id="@+id/movieDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="A top analyst for Homeland Security who tracks threats through a mass surveillance program, until a new threat breaks all the rules of war."
                android:textColor="#D8E7FB"
                android:textSize="16sp"
                android:lineSpacingExtra="8dp"
                android:padding="20dp"
                android:fontFamily="sans-serif"
                android:maxLines="5"
                android:ellipsize="end" />
        </androidx.cardview.widget.CardView>

    </LinearLayout>
</ScrollView>
